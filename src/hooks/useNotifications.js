import { useEffect } from 'react';
import { useAppStore } from '@/store/appStore';

export const useNotifications = () => {
  // Notifications will be handled via Supabase realtime in the future
  // For now, this is a no-op
};
